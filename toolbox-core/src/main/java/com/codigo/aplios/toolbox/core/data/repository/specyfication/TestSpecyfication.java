package com.codigo.aplios.toolbox.core.data.repository.specyfication;

// public class TestSpecyfication {
//
// public static void main(String[] args) {
//
// Poll myPoll = new Poll();
//
// boolean isPopular = new IsPopular().isSatisfiedBy(myPoll);
// boolean isCurrentlyRunning = new IsCurrentlyRunning().isSatisfiedBy(myPoll);
//
// Specification<Poll> popularAndRunning = new IsPopular().and(new IsCurrentlyRunning());
// boolean isPopularAndRunning = popularAndRunning.isSatisfiedBy(myPoll);
// List<Poll> polls = myRepository.findAllBySpecification(popularAndRunning);
//
// }
// }
